{
  "name": "linkpendek",
  "version": "1.0.0",
  "private": true,
  "description": "URL shortener dengan analitik klik (API + worker + frontend statis)",
  "engines": { "node": ">=20" },
  "scripts": {
    "start:api": "node src/server.js",
    "start:worker": "node src/worker.js",
    "migrate": "node scripts/migrate.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ioredis": "^5.4.1",
    "pg": "^8.12.0",
    "prom-client": "^15.1.3"
  }
}
